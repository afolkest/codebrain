"""codebrain — searchable store of coding-agent sessions.

The normalized layer follows SCHEMA.md: deduped `events` (content),
`session_events` (per-session placement = the forest), and `sessions`.
"""

__version__ = "0.0.1"
